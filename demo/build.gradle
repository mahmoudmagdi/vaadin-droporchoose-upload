plugins {
    // Uncomment this if Vaadin plugin is not applied in the parent module
    // id "fi.jasoft.plugin.vaadin" version "1.1.6"
}

// Apply the Vaadin plugin
apply plugin: 'fi.jasoft.plugin.vaadin'

vaadin {
    version '8.0.3'
    logToConsole true
}
// Demo depends on the addon
dependencies {
    compile project(':addon')
}
